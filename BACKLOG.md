# BACKLOG — work top to bottom, commit+push after each item, never stop
1. MCP/connectors tab (BRIEF-v2 #12): Hermes config.yaml + Claude Desktop first. Per-app writers, Hermes catalog = source. Tests + docs.
2. test_core_api.py python 3.9 compat (no sys.stdlib_module_names — fallback set).
3. Shareable preset export/import files (BRIEF-v2 #7).
4. Demo GIF + plugin-index submission leftovers.
5. Polish pass: responsive rows, empty states, toasts. Fix any CI red immediately, always.
RULE: finishing an item means starting the next. QUESTIONS_FOR_HERMES.md for blockers without stopping.
